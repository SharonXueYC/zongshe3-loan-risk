# API 网关（平台 · 薛雨宸）

独立 Spring Boot 进程，默认端口 **8088**。APP / Web 以后只连这个地址，由网关再转到信贷 / 风控 / 平台服务。

本周第 2 步：空网关能在 8088 启动。
本周第 3 步：把 `/api/users/**`、`/api/admin/**`、`/api/auth/**` 转到本机 `http://127.0.0.1:8080`（现有单体）。

